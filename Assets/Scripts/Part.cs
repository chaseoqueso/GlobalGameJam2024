using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Part : MonoBehaviour
{

    #region Stats
    public float speed;  //Determined max speed
    public float weight;  //Reduces knockback distance and damage taken
    public float attack;  //Increases knockback distance and damage done
    public float handling;  //Movement acceleration
    #endregion

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
